# gauge-vscode

## Installation

```shell 
$ npm install 
$ npm install -g vsce 
$ vsce package 
```

This will create `gauge-<version>.vsix` file which can be installed via VScode's `Install from VSIX`.
